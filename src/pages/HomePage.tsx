import React from 'react';
import SearchBar from '../features/SearchBar/SearchBar';

const HomePage = () => (
    <div className="flex flex-col items-center w-3/5 h-screen items-center py-20 bg-gray-200">
        <SearchBar />
    </div>
);
export default HomePage;
